import { join } from 'path';

export const SERVER_ROOT = join(__dirname, '..');
export const PROJECT_ROOT = join(__dirname, '../..');
export const PUBLIC_ROOT = join(PROJECT_ROOT, 'public/');
export const LOG_ROOT = join(PROJECT_ROOT, 'log/');
